import { ExporterOrImporter } from './exporter-or-importer';

describe('ExporterOrImporter', () => {
  it('should create an instance', () => {
    expect(new ExporterOrImporter()).toBeTruthy();
  });
});
